%% muitoolbox abstract classes
% The muitoolbox comprises a number of classes that collectively allow
% bespoke User Interfaces (UIs) to be rapidly built and customised.

%% Abstract Classes
% muitoolbox classes include the following abstract classes:
%%
% * <matlab:doc('muimodelui') muiModelUI>: defines a basic stucture for a modelling UI, with methods to initialise menus and tabs. 
% * <matlab:doc('muipropertyui') muiPropertyUI>: defines a default interface, with methods to handle and display model parameter input. 
% * <matlab:doc('muidataui') muiDataUI>: defines a UI for data selection that is implemented to suit specific uses (see some of the utlity classes below that make use of _muiDataUI_). 
% * <matlab:doc('muidataset') muiDataSet>: defines methods to manipulate datasets and is implemented by clasess for imported data and models (see _muiUserData_ and _muiUserModel_ below).  

%% See Also
% See <matlab:doc('muitoolbox') muitoolbox>, <matlab:doc('dstoolbox') dstoolbox>
% and documentation for the <matlab:doc('modelui') ModelUI> App.
